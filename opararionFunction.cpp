#include <bits/stdc++.h>
using namespace std;
int main()
{
  list<int> myList = {10, 1, 200, 10, 12, 2, 1, 3, 12, 4, 5, 6, 7, 12};
  // myList.remove(12);
  // myList.sort();
  // myList.sort(greater<int>());
  // myList.unique();
  // myList.reverse();
  // cout << myList.front() << endl;
  // cout << myList.back() << endl;
  // cout << *next(myList.begin(), 2) << endl;
  // for (int val : myList)
  // {
  //   cout << val << endl;
  // }
  return 0;
}
